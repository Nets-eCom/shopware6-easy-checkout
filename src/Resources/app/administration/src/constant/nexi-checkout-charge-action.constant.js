export const ACTION = Object.freeze({
    NEXI_CHECKOUT_CHARGE: 'action.nexi-checkout.charge',
});

export const GROUP = 'order'

export const CUSTOM_FIELDS_PAYMENT_ID = 'nexi_checkout_payment_id'

export default {
    ACTION, GROUP
};
